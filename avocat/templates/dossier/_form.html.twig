<style>

* {
    border: 0;
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
  
 button:hover{color:blue;}
  body{background-image: url("../img/aa.jpg");}
  img{height: 100%;
    width: 100%;}
 
  
  
 
</style><center>{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}</center>
